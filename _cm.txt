feat: 零登录投稿后端(不碰 GitHub)

用户只需在页面上填曲名(可选填简谱数字)点提交 -> 服务端把投稿落 feedback/ 留档,
有数字则直接生成 scores/曲名.txt 并本地 git commit。省掉 GitHub 的四重漏斗
(链接 -> 账号 -> 意愿 -> 流程), GH Issue 降级为"服务不在线时的兜底链接"。

- POST /api/submit {kind,title,score,note,contact}; GET /api/health
- 数字归一化: 一整串 63731232 自动拆成 6 3 7 3 1 2 3 2(逐 token 白名单才认)
- 可选 JPSUBMIT_TOKEN 保护; 默认仅监听 127.0.0.1
- 页面新增投稿表单(推荐收录 / 纠错 / 元数据), 并有"用刚才查询的曲名填入"

Co-authored-by: deepseek-ai <service@deepseek.com>
