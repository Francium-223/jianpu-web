{
  "name": "jianpu-web",
  "private": true,
  "type": "module",
  "description": "简谱旋律查歌 —— 前端（零依赖 ES module）+ Cloudflare Workers 部署包装",
  "scripts": {
    "build": "node tools/build_dist.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "check": "bash tools/check_all.sh"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
